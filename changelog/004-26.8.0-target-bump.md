---
id: 004
date: 2026-08-26
package-under-test: 26.8.0
type: change
---

# Target GroupDocs.Comparison.Mcp 26.8.0

## What changed
- Bumped `<McpPackageVersion>` and the CI defaults/fallbacks to `26.8.0` — `Directory.Build.props`, `.github/workflows/integration.yml` (`workflow_dispatch` default + `MCP_PACKAGE_VERSION` fallback), and `.github/copilot-instructions.md`.
- Re-pinned the example configs and how-to guides to `@26.8.0` — `examples/*.json`, `how-to/*` guides, `how-to/README.md`, `README.md`, and `AGENTS.md`. (`docker-scripts/*` illustrative pins left as-is.)

## Why
Keeps the integration suite pinned to the just-released server version. MCP 26.8.0 bumps the engine to GroupDocs.Comparison 26.8.0, which fixes the `Comparer` thread-safety bug (concurrent comparisons in one process) that affected 26.7.x on all platforms; the suite now exercises that release.

## Migration / impact
Test/doc-only change. No change to the tools exercised or their assertions.
