---
id: 003
date: 2026-07-15
package-under-test: 26.7.0
type: change
---

# Target GroupDocs.Comparison.Mcp 26.7.0 + add Cursor how-to

## What changed
- Bumped `<McpPackageVersion>` and every version reference across the suite to `26.7.0` — `Directory.Build.props`, `.github/workflows/integration.yml` defaults, `AGENTS.md`, `docker-scripts/README.md`, `examples/*.json`, and the `how-to/*` guides (the how-to guides were also re-pinned off the older `@26.5.0` references).
- Added [how-to/07-use-with-cursor.md](../how-to/07-use-with-cursor.md) and [examples/cursor-mcp.json](../examples/cursor-mcp.json) — Cursor uses the `mcpServers` key (like Claude Desktop) and needs the Windows `dnx` SSL/timeout workaround documented for the other clients. Adapted for Comparison's `compare` / `get_document_info` surface and its watermark-in-eval (never blocked) semantics.
- `ToolDiscoveryTests` already asserts the tool count is 2 (`Compare`, `GetDocumentInfo`) — unchanged.

## Why
Keeps the integration suite pinned to the just-released server version and rounds out the client-setup guides with Cursor, matching the other GroupDocs MCP Tests repos.

## Migration / impact
Test/doc-only change. No change to the tools exercised or their assertions.
